{
  "name": "widgetconverter",
  "version": "1.0.0",
  "description": "Simple widget that can be embedded on a web page. Purpose is to convert numbers from one numeral system to another.",
  "main": "app.js",
  "scripts": {
    "serve": "parcel src/index.html",
    "prod": "parcel build src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dominik Jurkowski",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {}
}
